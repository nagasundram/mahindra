# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rails secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

# Shared secrets are available across all environments.

# shared:
#   api_key: a1B2c3D4e5F6

# Environmental secrets are only available for that specific environment.

development:
  admin_name: 'Admin'
  admin_email: 'admin@mahindraretail.com'
  admin_password: 'password'
  email_provider_username: <%= ENV["GMAIL_USERNAME"] %>
  email_provider_password: <%= ENV["GMAIL_PASSWORD"] %>
  domain_name: giftcard.mahindraretail.com
  secret_key_base: d12c8e3355ce2a6f97fa7d0d9e0eb535e923f09f1b0b5607da5ab08572de22d97e48e261d7c5fa3cc031e3b1bb2288ec7b0644299331ff09ef57d3a191206f85


production:
  admin_name: "Admin"
  admin_email: 'admin@mahindraretail.com'
  admin_password: 'password'
  email_provider_username: <%= ENV["GMAIL_USERNAME"] %>
  email_provider_password: <%= ENV["GMAIL_PASSWORD"] %>
  domain_name: "giftcard.mahindraretail.com"
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
